{
  "$schema": "yos-agents-capability-schema-v1.0.0",
  "capability_id": "memory",
  "name": "Memory",
  "description": "Store, retrieve, and synthesize information across sessions; maintain persistent context",
  "input_types": ["text", "structured_data", "session_context"],
  "output_types": ["context_pack", "memory_entry", "synthesis"],
  "risk_level": "medium",
  "requires_auth": false,
  "requires_filesystem": true,
  "requires_network": false,
  "requires_human_confirmation": false,
  "validated_status": "candidate",
  "notes": "Candidate for all agents via external memory systems (Notion, Mem0, Git). Proven for Manus via /home/ubuntu/ persistence. ChatGPT has native memory (limited). Claude/Gemini: session-scoped only without external tools.",
  "memory_backends": {
    "notion": "candidate",
    "mem0": "candidate",
    "git": "proven",
    "filesystem": "proven"
  }
}
