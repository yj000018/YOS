# mpm.py — Tests

Run from repo root:

```bash
python -m pytest 01_BACKBONE/MPM/08_TOOLS/tests/ -v
# or without pytest:
python 01_BACKBONE/MPM/08_TOOLS/tests/test_mpm_cli.py
```

Tests are stdlib-only (no pytest required for basic run).
